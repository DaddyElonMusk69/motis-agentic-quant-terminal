interface:
  display_name: "Stage 1A Training Optimizer"
  short_description: "Regime-first Stage 1A strategy research and patching."
  default_prompt: "Use $stage1a-training-optimizer to update a Stage 1A strategy from training evidence by proving broad signal-time regime patterns before patching the rules."
